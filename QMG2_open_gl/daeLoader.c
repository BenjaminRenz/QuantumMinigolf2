#include <stdio.h>
read_UTF8_file_to_UTF32(filepointer* fp){
    #define readBufferSize 2048
    uint8_t* readBuffer=(uint8_t*) malloc(2048);
    fread(readBuffer,)
    uint8_t currentBufferIndex=0;
    if(readBuffer[currentBufferIndex]>0x7F){

    }else{

    }
    fclose(fp);
}
